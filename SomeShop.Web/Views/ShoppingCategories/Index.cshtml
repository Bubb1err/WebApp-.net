@model IEnumerable<Category>
<div class="container">
    @foreach (var category in Model) {
        <div class="row-4">
            <a href="#" class="col">
                @category.Name
            </a>
        </div>
    }
</div>

